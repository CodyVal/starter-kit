{
  "name": "starter-kit",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev webhooks",
    "only:dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "webhooks": "turbo webhooks",
    "types:generate": "turbo types:generate --ui stream"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "localtunnel": "^2.0.2",
    "supabase": "^2.1.1"
  }
}
